Place your app icons here:

- icon.png   (512x512)   — Linux + development fallback
- icon.ico   (multi-res) — Windows
- icon.icns  (512+1024)  — macOS

Generate from a single 1024x1024 PNG:
  npx electron-icon-builder --input=icon-source.png --output=.
